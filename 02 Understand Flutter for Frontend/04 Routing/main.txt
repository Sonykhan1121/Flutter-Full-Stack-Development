import 'package:flutter/material.dart';
import 'on_generate_route.dart'; // Import the file where OnGenerateRoute is defined
import 'home_page.dart'; // Import the home page

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Flutter Demo',
      theme: ThemeData(
        primarySwatch: Colors.blue,
      ),
      initialRoute: '/', // Set the initial route
      onGenerateRoute: OnGenerateRoute.route, // Use OnGenerateRoute for route generation
    );
  }
}